# CLAUDE.md

See [`AGENT.md`](AGENT.md) for full project context — architecture, key
decisions, running instructions, and known gaps. It's kept as the single
source of truth for AI agents working in this repo; this file just points
to it so both `CLAUDE.md`-aware and `AGENT.md`-aware tooling find it.

The backend API contract this app implements against lives at
[`docs/backend-api-contract.md`](docs/backend-api-contract.md) — read it
before touching any networking or data-model code.
